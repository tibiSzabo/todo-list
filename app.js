import { TodoItem } from 'todo.js';

document.addEventListener('DOMContentLoaded', function(){
    const todoList = [];

    const toggleTodoInput = () => {
        
    }

    document.querySelector('#addTodo').addEventListener('click', toggleTodoInput);

});